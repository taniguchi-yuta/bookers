<% if flash[:notice] %>
	<div class="flash">
		<%= flash[:notice] %>
	</div>
<% end %>
<div class="show">
<p class="title"><span>Title:</span><%= @book.title %></p>
<p class="body"><span>Body:</span><%= @book.body %></p>
</div>

<%= link_to "Edit", edit_book_path(@book.id) %>
<span>|</span>
<%= link_to "Back", books_path %>